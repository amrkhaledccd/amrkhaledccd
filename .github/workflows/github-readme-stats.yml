name: Generate GitHub Readme Stats

on:
  schedule:
    - cron: '0 0 * * *' # Runs every day at midnight
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - rescue-stats:
        uses: anuraghazra/github-readme-stats@master
        with:
          USERNAME: amrkhaledccd
